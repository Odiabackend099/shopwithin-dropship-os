{
  "buildCommand": "pnpm --filter @dropship-os/core build && pnpm --filter @dropship-os/admin build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": "apps/admin/.next"
}
